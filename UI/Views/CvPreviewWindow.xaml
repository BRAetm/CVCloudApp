<Window x:Class="LabsVision.UI.Views.CvPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TM Labs — CV Preview"
        Width="640" Height="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="True"
        MouseLeftButtonDown="OnDrag">

    <Border CornerRadius="10" BorderThickness="1" BorderBrush="#1565C0" ClipToBounds="True">
        <Border.Background>
            <SolidColorBrush Color="#0A0E1A"/>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="#0D1525">
                <Grid Margin="10,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="#00E676" Margin="0,0,6,0"/>
                        <TextBlock x:Name="TitleText" Text="CV Preview" Foreground="#42A5F5"
                                   FontSize="11" FontWeight="SemiBold"/>
                        <TextBlock x:Name="FpsText" Text="" Foreground="#4A6A8A"
                                   FontSize="10" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <!-- Minimize — hides window but CV keeps running -->
                        <Button Click="OnMinimize" Cursor="Hand" ToolTip="Minimize (CV keeps running)">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="Transparent" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="&#x2014;" Foreground="#4A6A8A" FontSize="10"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1A2744"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <!-- Close -->
                        <Button Click="OnClose" Cursor="Hand" ToolTip="Close preview" Margin="2,0,0,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Bd" Background="Transparent" CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="&#x2715;" Foreground="#4A6A8A" FontSize="10"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#2A1520"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- CV Frame -->
            <Image x:Name="CvImage" Grid.Row="1"
                   Stretch="Uniform"
                   RenderOptions.BitmapScalingMode="LowQuality"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch"/>
        </Grid>
    </Border>
</Window>
